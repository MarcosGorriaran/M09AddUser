@page
@model AddUser.Pages.Clients.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Client[0].DNI)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Client[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Client[0].SubDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Client[0].ModDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Client) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DNI)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DNI">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DNI">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DNI">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
